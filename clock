OD=""
while true
do
	D=`date "+%T %Z"`
	if [ "$D" != "$OD" ]
	then
		[ -z "$OD" ] || echo -n "[9A"
		echo "$D" | banner -f | sed '1s/^/[32m/
									 $s/$/[m/
									 s/#/[36m&/
									 s/|$/[32m|/
									 s/#/O/g'
	else
		sleep 0.1
	fi
	OD=$D
done
